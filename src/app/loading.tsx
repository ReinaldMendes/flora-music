export default function Loading() {
  return (
    <div className="fixed inset-0 bg-flora-offwhite flex items-center justify-center z-50">
      <p className="font-display text-3xl text-flora-deep opacity-60 animate-pulse">Flora</p>
    </div>
  )
}
